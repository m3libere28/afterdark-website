const video=document.getElementById('walkthrough');const status=document.getElementById('video-status');document.querySelectorAll('[data-chapter]').forEach(button=>button.addEventListener('click',async()=>{document.getElementById('demo').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});const seek=()=>{video.currentTime=Number(button.dataset.chapter);};if(video.readyState>=1)seek();else{video.addEventListener('loadedmetadata',seek,{once:true});video.load();}document.querySelectorAll('.chapters button').forEach(b=>b.classList.toggle('active',b.dataset.chapter===button.dataset.chapter));try{await video.play();status.textContent='';}catch{status.textContent='Press play on the video to watch this chapter.';}}));